<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/card_shortcut_width"
    android:layout_height="@dimen/card_shortcut_height"
    android:layout_marginEnd="@dimen/margin_medium"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.Trailerx.Shortcut"
    android:elevation="@dimen/elevation_medium">

    <TextView
        android:id="@+id/text_box_shortcut"
        android:layout_width="match_parent"
        android:layout_height="@dimen/label_shortcut_height"
        app:layout_constraintTop_toTopOf="parent"
        style="@style/Widget.Trailerx.Shortcut.Small"
        android:layout_marginStart="@dimen/margin_medium"
        android:layout_marginTop="@dimen/margin_medium"
        android:layout_marginEnd="@dimen/margin_medium"
        android:paddingTop="@dimen/margin_medium"
        android:paddingStart="@dimen/padding_small"
        android:paddingEnd="@dimen/padding_small"
        android:textAppearance="@style/TextAppearance.Trailerx.Body.Small"
        tools:text="Calificar y obtener recomendaciones"/>

    <TextView
        android:id="@+id/title_shortcut"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/text_box_shortcut"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="@dimen/margin_small"
        android:layout_marginStart="@dimen/margin_medium"
        android:layout_marginEnd="@dimen/margin_medium"
        android:textAppearance="@style/TextAppearance.Trailerx.Body.Small"
        tools:text="Calificaciones"/>

    <TextView
        android:id="@+id/score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_medium"
        android:textAppearance="@style/TextAppearance.Trailerx.Body.SmallLight"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/title_shortcut"
        tools:ignore="TextContrastCheck"
        tools:text="0" />

</androidx.constraintlayout.widget.ConstraintLayout>